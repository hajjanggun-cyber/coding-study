# 📘 코딩 용어 정리 시스템 - 사용자 가이드

**버전**: 2.0  
**최종 업데이트**: 2026-02-15

---

## 1. 프로젝트 소개
이 프로젝트는 코딩 공부를 하면서 알게 된 **용어, 명령어, 유용한 사이트, 아이디어** 등을 **구글 스프레트시트(엑셀)**에 자동으로 저장하고 관리해주는 나만의 웹 지식 저장소입니다.

복잡한 데이터베이스 서버 비용 없이, **HTML 파일 하나**와 **구글 무료 계정**만 있으면 평생 무료로 강력한 기능을 사용할 수 있습니다.

---

## 2. 핵심 기능

### ✅ 10가지 주제별 관리
상단 탭을 눌러 주제별로 데이터를 분리하여 관리합니다.
- **기본형**: Coding용어, 프롬프트, 참고URL, 아이디어, 손님내용, 방문예정, 물건세부
- **확장형 (더보기 지원)**: 실전과정, 강의정리, 개인자료

### ✅ 안전한 데이터 관리 (업그레이드됨!)
- **고유 ID 시스템**: 데이터가 꼬이지 않도록 등록 시점의 1/1000초 단위 시간(Timestamp)을 ID로 부여합니다.
- **비밀번호 잠금**: 앱 접속 시 비밀번호(기본: `0828`)를 입력해야 내용을 볼 수 있습니다. (브라우저에 자동 저장 가능)

### ✅ 정렬과 순서 변경 (중요!)
이 프로그램은 세 가지 보기 방식을 제공합니다.

| 정렬 방식 | 설명 | 순서 변경 가능 여부 |
| :--- | :--- | :---: |
| **입력순** | **(기본)** 엑셀에 저장된 실제 순서대로 보여줍니다. (1번행, 2번행...) | **가능 (O)** |
| **최신순** | **(추천)** 가장 최근에 등록한 내용이 맨 위에 보입니다. (입력순의 역순) | **가능 (O)** |
| **가나다순** | ㄱ, ㄴ, ㄷ 순서로 정렬해서 보여줍니다. (보기 전용) | **불가능 (X)** |

> **💡 팁:** 순서를 바꾸고 싶을 때는 반드시 **'입력순'** 또는 **'최신순'** 탭을 누른 상태에서 이동 버튼을 사용하세요. '가나다순'에서는 순서가 고정됩니다.

---

## 3. 사용 방법

### 1️⃣ 데이터 등록하기
1. 원하는 **탭**을 선택합니다.
2. **용어**와 **설명**을 입력합니다.
3. **[데이터 등록]** 버튼을 누릅니다.
4. 잠시 후 리스트 최상단에 추가됩니다. (최신순 기준)

### 2️⃣ 순서 바꾸기 (위/아래 이동)
1. **'입력순'** 또는 **'최신순'** 버튼이 눌러져 있는지 확인합니다.
2. 이동할 항목의 **체크박스**를 1개만 선택합니다.
3. 우측 상단의 화살표 버튼을 누릅니다.
   - ⏫ : 맨 위로 이동
   - 🔼 : 한 칸 위로
   - 🔽 : 한 칸 아래로
   - ⏬ : 맨 아래로 이동
4. 화면에서 즉시 이동하고, **자동으로 서버(엑셀)에도 저장**됩니다.

### 3️⃣ 수정 및 삭제
- **수정**: `[수정]` 버튼을 누르면 그 자리에서 바로 내용을 고칠 수 있습니다.
- **삭제**: 체크박스를 선택하고 `[선택 삭제]`를 누르면 영구적으로 삭제됩니다.

---

## 4. 문제 해결 (Troubleshooting)

### ❓ 옛날 데이터가 수정/삭제가 안 돼요!
- **원인**: 예전에 등록한 데이터는 ID가 단순 숫자(예: 1, 2)라서, 현재 시스템의 정밀 ID와 호환되지 않을 수 있습니다.
- **해결 방법 (데이터 이사)**:
  1. 웹 화면에서 옛날 데이터 내용을 복사합니다.
  2. 위쪽 입력창에 붙여넣고 **[데이터 등록]**을 눌러 **새로 저장**합니다.
  3. 새 데이터가 잘 작동하는지 확인합니다.
  4. 구글 스프레드시트 파일을 열어서, 작동 안 하는 옛날 행(Row)을 **마우스 우클릭 > 삭제**로 지워주세요.

### ❓ 순서 변경이 안 돼요!
- 혹시 **'가나다순'** 버튼이 눌러져 있지 않나요? **'최신순'** 버튼을 누르고 다시 시도해보세요.

---

## 5. 설치 방법 (초기 세팅)

1. **구글 스프레드시트 생성**: 새 시트를 만들고 메뉴의 `확장 프로그램` > `Apps Script` 실행.
2. **코드 붙여넣기**: `google-apps-script-참고.js` 내용을 복사해서 붙여넣고 저장.
3. **배포**: `배포` > `새 배포` > 유형: `웹 앱` > 액세스: `모든 사용자` 설정 후 배포.
4. **URL 연결**: 배포된 URL을 `index.html` 파일 안의 `API_URL` 변수에 붙여넣기.
5. **실행**: `index.html` 파일을 열면 끝!

---
**GitHub Repository**: https://github.com/hajjanggun-cyber/coding-study
